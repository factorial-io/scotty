.lint:docker-images:
  rules:
    - if: $TAG_VERSION_C
  script:
    - "phab --config=ci script lint:docker --no-interaction"
  tags:
    - fabalicious

.lint:yafbds:
  script:
    - "phab -cci script lint:yafbds --no-interaction"
  tags:
    - fabalicious
  rules:
    - if: $TAG_VERSION_C
