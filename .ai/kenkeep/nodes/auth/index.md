# kenkeep Index: auth

↑ Parent: [kenkeep](../index.md)

> kenkeep navigation: the injected body above is the root index node, the top-level catalog of branches and root-level leaves. Do not expect the whole knowledge base here; descend on demand. Read the root index node, pick one or more branches whose intent and tags match your task (several branches can be relevant), and read those branch `index.md` nodes. Descend further only where the task needs it, opening only the leaves you have confirmed are relevant. Follow each leaf's `relates_to` and `depends_on` cross edges to reach related leaves in other branches. You decide how deep to go per branch.

> This index only orients you; leaves hold the durable guidance. Open at least one relevant leaf before acting.

## Subfolders
- Load [`authorization/`](authorization/index.md) for more information on Casbin RBAC: built-in roles, assignment key formats per auth mode, precedence rules, and required token assignments; read when changing policy.yaml, roles, scopes, or permissions.
- Load [`bearer-tokens/`](bearer-tokens/index.md) for more information on bearer token naming, constant-time comparison, and hybrid-mode check order; read when touching bearer token auth or service-account access.
- Load [`oauth/`](oauth/index.md) for more information on OAuth route protection, secret storage, session lifetimes, and the redirect_url vs frontend_base_url distinction; read when changing OAuth flows or their configuration.
- Load [`rate-limiting/`](rate-limiting/index.md) for more information on per-tier rate limiting and its per-instance (not global) scope; read when changing rate limits or scaling the server.

## Conventions (how we build)
_None yet._

## Components (what exists)
_None yet._

## By topic

_No tags yet._